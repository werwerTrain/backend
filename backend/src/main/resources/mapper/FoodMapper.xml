<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.buaa.werwertrip.mapper.IFoodMapper">
    <insert id="addFoodOrder">
        insert into foodOrders(oid, foodName, count, trainId, trainDate, mealTime, mealDate, photo)
        VALUES (#{oid}, #{foodName}, #{count}, #{trainId}, #{trainDate}, #{mealTime}, #{mealDate}, #{photo})
    </insert>

    <select id="getTrainRelatedFoodOrders">
        select *
        from orders o
        where o.uid = #{uid}
          and exists(
            select *
            from foodOrders f
            join trainorders t on f.trainId = t.trainId and f.trainDate = t.trainDate
            where f.oid = o.oid
            and t.trainDate = #{date}
            and t.trainId = #{tid}
            )
    </select>
</mapper>
